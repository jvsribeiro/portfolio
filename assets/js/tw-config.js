// Tailwind config (custom container and colors if needed)
tailwind.config = {
  theme: {
    extend: {
      fontFamily: { sans: ['Inter', 'ui-sans-serif', 'system-ui', 'Segoe UI', 'Roboto', 'Helvetica', 'Arial', 'sans-serif'] },
      boxShadow: {
        soft: '0 10px 25px -10px rgb(0 0 0 / 0.25)'
      }
    }
  }
};


